<h1>Categories#index</h1>

<% @categories.each do |category| %>
  <%= link_to category.name,category_path(category) %>
  <ul>
  <% category.smallcategories.each do |sub_category| %>
    <li><%= link_to sub_category.name,subcategory_path(sub_category) %></li>
  <% end %>
  </ul>
<% end %>
